spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3305/sns?charcaterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password:

  jpa:
    generate-ddl: true